# Epic 12 — Context Cache

_Generated by aped-story at 2026-07-13T15:35:00Z. Consumers: aped-dev, aped-review. Do not hand-edit — re-run aped-story to refresh._

## Scope from PRD

- FRs in this epic: FR5 — System generates draft schedules highlighting staffing "holes" (re-covered at a higher optimality bar); FR8 — System flags "Soft Rule" violations (Overtime, Equity) for Admin review (re-covered: the equity objective now has an exact-optimization path). No new FRs — Epic 12 is the PRD Phase-3 Vision item *"AI Engine: Global optimization algorithms for complex fairness balancing"* made concrete.
- NFRs that bind: NFR2 — schedule generation < 2s with visual loading feedback beyond 1s (the solver runs inside this budget or is not served); NFR3 — zero silent failures, every solver outcome (timeout, infeasible, rejected plan) is visible in structured logs; NFR6 — multi-tenant isolation via `clinicId` on every query; NFR9 — 50 employees per clinic without degradation.

## Architecture references

- Patterns to respect: rigid data flow `Page → Client Component → Hook → Zsa → Server Action → tRPC Client → NestJS Service → Prisma` (architecture.md § Communication Patterns) — this story is backend-only, the flag rides the existing `planning.generatePlan` tRPC procedure; Zod validation single-sourced in `packages/validators` (architecture.md § Data Layer); one NestJS module per domain — all new files live in `apps/api/src/modules/planning/` (architecture.md § Backend Modularity).
- Pure-module discipline (established Epic 11): decision cores are pure TypeScript files beside the service (`rule-engine.ts`, `french-labor-law.ts`, `local-repair.ts`) — no NestJS, no Prisma, no I/O, no RNG, deterministic iteration. `solver-model.ts` follows it; `solver-engine.service.ts` is the injectable adapter and the ONLY importer of `or-tools-wasm`.
- Determinism invariant (epic-11 cache § 3, invariant 3): the generator is fully deterministic — the solver pass pins `numSearchWorkers = 1`, a fixed seed, and a deterministic-time budget (never wall-clock).
- Trigger.dev offload of Nest-DI code is DEAD (11-10 outcome: esbuild strips `emitDecoratorMetadata` → DI resolves undefined) — the solver pass is in-process and budgeted, never a Trigger task.
- KON-128 (quick-spec, PR #108) supplies `deriveEquityWeights` / `EquityWeights` / `equityObjective(loads, weights)` in `local-repair.ts` and the `buildDepth3CounterExample` spec fixture — the story's Task 1 is a merge gate on it.
- ADRs in scope: none (architecture.md Phase 2b records no council dispatches).

## UX references

- Screens: none — backend-only story; the flag is reachable via tRPC only, no admin UI toggle in this story (possible follow-up story adds the toggle per ux/flows.md § Admin Planning Generation Loop's "Transparency over Magic" principle).
- Components: none.

## Project context (brownfield only)

- Existing patterns the epic must respect: `apps/api/src/modules/planning/planning-generation.service.ts` is the single generation entry (`generateMonthlyPlan`); counters (`assignmentIndex`, `weeklyMinutesCounter`, equity/rotation indexes) are seeded from border + survivor shifts before the greedy loop and mutated in lockstep by `applyAssignment` / `removeAssignment`; every mutation path of a PUBLISHED month goes through the 7-6 guard (whole-surface invariant, epic-11 cache § 3.2); hours are net minutes (`breakMinutes` deducted) over ISO weeks in UTC; violation messages use structured `messageKey`/`messageParams`, never raw English strings; API tests are Jest `*.spec.ts`, validators tests are Vitest `*.test.ts`; all pnpm commands from repo root (root `pnpm test` broken by the rtk shim — use `--filter`).

## Lessons applicable

- L-audit — "verified" means every guard entry-point, not one. Scope: all. Applies here: the solver improve pass is a NEW code path that mutates the served plan — it must respect the 7-6 guard context (it runs inside `generateMonthlyPlan`, after the guard) and never bypass re-validation.
- L1 — Never mix Zsa tuple returns with TanStack Query direct returns. Scope: apps/web hooks. Not triggered (backend-only story).
- L2 — Unit/integration tests do not replace real user-journey testing. Scope: all. Applies: AC7 mandates the live tRPC journey on the seeded clinic.
- L3 — Reviews must cross-reference the PRD and architecture, not just the code. Scope: review. Carried to aped-review.
- L4 — Consult up-to-date docs (MCP Context7) for third-party SDKs; record sources in Dev Notes. Scope: all stories using third-party SDKs. Applies: `or-tools-wasm` API surface must be verified against its current README before the adapter is written (story Task 2 canary).
- L5 — SWC builds emit no `.d.ts`; the `tsc -p tsconfig.types.json` pass in `apps/api` build is load-bearing. Scope: monorepo build. Applies: story Task 12 runs the full build to prove or-tools-wasm's types don't break the declaration pass.

## Previous stories — outcomes

_(Appended by aped-review at story→done. Empty on first story of the epic.)_
